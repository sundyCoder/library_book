create table book
(
	sno integer primary key,
	name varchar(20),
	price float,
	author varchar(20),
	publish varchar(30)
);

insert into book values(null,'thinking in C++',110,'brucl','Wuhan publisher');
